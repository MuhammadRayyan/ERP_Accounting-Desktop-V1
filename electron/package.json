{
  "name": "electron-shell",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && electron .",
    "dist": "npm run build && electron-builder"
  },
  "build": {
    "appId": "com.mycompany.accountingapp",
    "files": ["dist/**/*", "assets/**/*"],
    "directories": {
      "buildResources": "assets"
    },
    "win": {
      "target": ["nsis"]
    }
  },
  "devDependencies": {
    "@types/node": "...",
    "electron": "...",
    "electron-builder": "...",
    "ts-node": "...",
    "typescript": "..."
  },
  "dependencies": {}
}
